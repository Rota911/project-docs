# ls_density

### shared/main.lua

```lua
Config = {}

-- Globális beállítások
Config.GlobalSettings = {
    vehicles = 0.3,        -- Járművek (0.0 - 1.0)
    peds = 0.3,            -- Gyalogosok (0.0 - 1.0)
    parked = 0.5,          -- Parkolt járművek (0.0 - 1.0)
    scenarios = true,      -- Ambient scenarios
    suppressTraffic = false -- Forgalom teljes letiltása
}

-- Zóna alapú beállítások
Config.Zones = {
    -- Városközpont - magas density
    {
        name = 'Downtown',
        coords = vector3(-269.9, -955.1, 31.2),
        radius = 500.0,
        settings = {
            vehicles = 0.8,
            peds = 0.8,
            parked = 0.9,
            scenarios = true
        }
    },
    
    -- Airport - közepes density
    {
        name = 'Airport',
        coords = vector3(-1037.7, -2738.5, 13.8),
        radius = 800.0,
        settings = {
            vehicles = 0.5,
            peds = 0.4,
            parked = 0.6,
            scenarios = true
        }
    },
    
    -- Sandy Shores - alacsony density
    {
        name = 'Sandy Shores',
        coords = vector3(1960.5, 3740.7, 32.3),
        radius = 1000.0,
        settings = {
            vehicles = 0.2,
            peds = 0.1,
            parked = 0.3,
            scenarios = false
        }
    },
    
    -- Mission areas - nincs NPC
    {
        name = 'Mission Zone',
        coords = vector3(0.0, 0.0, 0.0),
        radius = 100.0,
        settings = {
            vehicles = 0.0,
            peds = 0.0,
            parked = 0.0,
            scenarios = false,
            suppressTraffic = true
        }
    }
}

-- Jármű típusok tiltása
Config.BlacklistedVehicles = {
    `RHINO`,        -- Tank
    `INSURGENT`,    -- Páncélozott
    `SAVAGE`,       -- Helikopter
    `HYDRA`,        -- Vadászgép
    `ANNIHILATOR`   -- Katonai helikopter
}

-- NPC típusok tiltása
Config.BlacklistedPeds = {
    `s_m_m_security_01`,    -- Security
    `s_m_y_cop_01`,         -- Cop
    `s_m_y_swat_01`         -- SWAT
}

-- Idő alapú beállítások
Config.TimeBasedSettings = {
    -- Éjszaka (22:00 - 06:00)
    night = {
        hours = {22, 23, 0, 1, 2, 3, 4, 5, 6},
        settings = {
            vehicles = 0.2,
            peds = 0.1,
            parked = 0.7,
            scenarios = false
        }
    },
    
    -- Rush hour (07:00 - 09:00, 17:00 - 19:00)
    rush = {
        hours = {7, 8, 9, 17, 18, 19},
        settings = {
            vehicles = 0.9,
            peds = 0.6,
            parked = 0.3,
            scenarios = true
        }
    },
    
    -- Nappal (10:00 - 16:00, 20:00 - 21:00)
    day = {
        hours = {10, 11, 12, 13, 14, 15, 16, 20, 21},
        settings = {
            vehicles = 0.5,
            peds = 0.5,
            parked = 0.5,
            scenarios = true
        }
    }
}
```

## Használat

### Density beállítása

```lua
-- Globális density
exports.ls_density:SetVehicleDensity(0.5)  -- 50% járművek
exports.ls_density:SetPedDensity(0.3)      -- 30% gyalogosok
exports.ls_density:SetParkedDensity(0.7)   -- 70% parkolt járművek

-- Scenarios toggle
exports.ls_density:SetScenarios(true)  -- Bekapcsolás
exports.ls_density:SetScenarios(false) -- Kikapcsolás

-- Teljes forgalom letiltás
exports.ls_density:SuppressTraffic(true)
```

### Zóna alapú kezelés

```lua
-- Zóna hozzáadása runtime-ban
exports.ls_density:AddZone({
    name = 'Custom Zone',
    coords = vector3(100.0, 200.0, 30.0),
    radius = 250.0,
    settings = {
        vehicles = 0.0,
        peds = 0.0,
        parked = 0.0,
        scenarios = false,
        suppressTraffic = true
    }
})

-- Zóna eltávolítása
exports.ls_density:RemoveZone('Custom Zone')

-- Zóna módosítása
exports.ls_density:UpdateZone('Custom Zone', {
    vehicles = 0.5,
    peds = 0.5
})
```


#### Set Vehicle Density

```lua
exports.ls_density:SetVehicleDensity(density)
-- density: 0.0 - 1.0
```

#### Set Ped Density

```lua
exports.ls_density:SetPedDensity(density)
-- density: 0.0 - 1.0
```

#### Set Parked Density

```lua
exports.ls_density:SetParkedDensity(density)
-- density: 0.0 - 1.0
```

#### Set Scenarios

```lua
exports.ls_density:SetScenarios(enabled)
-- enabled: true/false
```

#### Suppress Traffic

```lua
exports.ls_density:SuppressTraffic(suppress)
-- suppress: true/false
```

#### Add Zone

```lua
exports.ls_density:AddZone(zoneData)
```

#### Remove Zone

```lua
exports.ls_density:RemoveZone(zoneName)
```

#### Update Zone

```lua
exports.ls_density:UpdateZone(zoneName, settings)
```

#### Get Current Settings

```lua
local settings = exports.ls_density:GetCurrentSettings()
-- returns: {vehicles, peds, parked, scenarios, suppressTraffic}
```
